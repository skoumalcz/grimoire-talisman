plugins {
    id 'project.build'
}

android {
    kotlinOptions {
        freeCompilerArgs = ["-Xopt-in=kotlin.RequiresOptIn"]
    }
}

dependencies {

    debugImplementation(project(":talisman-seal"))

    releaseImplementation("com.skoumal.grimoire:talisman-seal:${getProperty("version")}")

}