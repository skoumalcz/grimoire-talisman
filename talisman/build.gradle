plugins {
    id 'project.build'
}

android {
    kotlinOptions {
        freeCompilerArgs = ["-Xopt-in=kotlin.RequiresOptIn"]
    }
}

dependencies {

    implementation(project(":talisman-seal"))

}