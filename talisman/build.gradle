plugins {
    id 'project.build'
    id("com.vanniktech.maven.publish")
}

android {
    kotlinOptions {
        freeCompilerArgs = ["-Xopt-in=kotlin.RequiresOptIn"]
    }
}

dependencies {

    api(project(":talisman-seal"))

}